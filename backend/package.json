{
  "name": "dify-evaluation-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend for Dify evaluation results visualization dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node check-environment.js",
    "setup": "npm install && npm run check",
    "rebuild": "npm rebuild better-sqlite3",
    "port": "node port-manager.js",
    "kill-port": "node port-manager.js 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
